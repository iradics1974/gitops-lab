apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: argocd-application-controller-extra
  labels:
    app.kubernetes.io/name: argocd-application-controller
    app.kubernetes.io/part-of: argocd
rules:
  # 🔥 Engedélyezzük az ArgoCD-nek, hogy más Application objektumokat módosítson
  - apiGroups:
      - argoproj.io
    resources:
      - applications
    verbs:
      - get
      - list
      - watch
      - update      # <-- EZ hiányzott
      - patch       # <-- EZ hiányzott
